# Notification Service

A lightweight notification system used internally to alert users about account events such as
password resets, order confirmations, and promotional campaigns.

## What it does
- Sends email and SMS notifications to registered users
- Logs all sent notifications to a file for audit purposes
- Formats messages differently based on notification type (transactional vs. marketing)

## Running
Compile all `.java` files and run `Main.java`.
