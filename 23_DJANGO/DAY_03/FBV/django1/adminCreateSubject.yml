info:
  name: adminCreateSubject
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/subjects/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
